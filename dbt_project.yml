name: 'dbt_takehome'
version: '1.0.0'

profile: 'dbt_takehome'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_takehome:
    staging:
      +materialized: view

    snapshot_models:
      +materialized: table

    intermediate:
      +materialized: view

    marts:
      +materialized: table
